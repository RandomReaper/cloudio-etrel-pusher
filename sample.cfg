#
# Sample config file
#

[cloudio]
name = 'cloudio'
host = 'cloudio-data.esr.ch'
port = 8883

#username MUST match endpoint name
username = 'cs-pusher'
password = ''
ca = 'certs/ca-cert.pem'
cert = 'certs/sample-cert.pem'
key = 'certs/sample-key.pem'

# endpoint name MUST match CN in certificate!
[endpoint]
name = cs-pusher
